Command: export PATH="/workspace/repo/.helix-venv/bin:/workspace/repo/venv/bin:/workspace/repo/.venv/bin:/workspace/.helix-venv/bin:$PATH" && cd /workspace/repo && grep -n "name-tile\.\|name-tile \.\|\.done\|\.locked\|\.late\|\.absent\|\.present\|\.pending" styles.css && echo "=====MODAL/TOAST/CHIP=====" && grep -n -A6 "\.modal \|\.modal{\|\.modal\b" styles.css | head -50 && echo "=====" && grep -n -A5 "\.toast\b" styles.css && echo "=====" && grep -n -A5 "summary-chip\|tile-badge\|kbd-hint\|pin-feedback\|pin-dot\b\|pin-dots\|keypad\b\|kiosk-summary\|toolbar\b\|history-table" styles.css | head -80
Cwd: /workspace/repo

=== STDOUT ===
149:.name-tile .initials {
154:.name-tile .name { font-weight: 600; text-align: center; }
155:.name-tile .meta { font-size: 12px; color: var(--muted); }
156:.name-tile.state-present { border-color: var(--ok); background: rgba(46, 204, 143, 0.08); cursor: default; }
157:.name-tile.state-present .tile-badge { color: var(--ok); }
242:.status-pill.present { background: rgba(46, 204, 143, 0.15); color: var(--ok); }
243:.status-pill.absent { background: rgba(255, 107, 107, 0.15); color: var(--bad); }
244:.status-pill.late { background: rgba(245, 184, 61, 0.15); color: var(--late); }
=====MODAL/TOAST/CHIP=====
288:.modal-root {
289-  position: fixed; inset: 0; z-index: 50;
290-  display: flex; align-items: center; justify-content: center;
291-  background: rgba(5, 8, 15, 0.7);
292-}
293:.modal {
294-  background: var(--panel); border: 1px solid var(--border);
295-  border-radius: var(--radius); box-shadow: var(--shadow);
296-  width: min(520px, 92vw); max-height: 86vh; overflow: auto;
297-  padding: 20px;
298-}
299:.modal h2 { margin: 0 0 14px; font-size: 18px; }
300:.modal .form-stack { display: flex; flex-direction: column; gap: 12px; }
301:.modal .form-row { display: flex; gap: 10px; flex-wrap: wrap; }
302:.modal .actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 18px; }
303-
304-.session-banner {
305-  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
306-  background: var(--panel); border: 1px solid var(--border);
307-  padding: 10px 14px; border-radius: var(--radius); margin-bottom: 12px;
308-  font-size: 14px;
=====
279:.toast {
280-  position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%);
281-  background: var(--panel-2); border: 1px solid var(--border);
282-  padding: 12px 20px; border-radius: 10px; box-shadow: var(--shadow);
283-  z-index: 60; font-size: 14px;
284-}
285:.toast.ok { border-color: var(--ok); }
286:.toast.err { border-color: var(--bad); }
287-
288-.modal-root {
289-  position: fixed; inset: 0; z-index: 50;
290-  display: flex; align-items: center; justify-content: center;
291-  background: rgba(5, 8, 15, 0.7);
=====
157:.name-tile.state-present .tile-badge { color: var(--ok); }
158:.tile-badge { position: absolute; top: 8px; right: 10px; font-size: 18px; }
159-
160-/* PIN pad */
161-.pin-stage { max-width: 420px; margin: 0 auto; text-align: center; }
162-.pin-stage .student-name { font-size: 20px; font-weight: 700; margin: 8px 0 2px; }
163:.pin-dots { display: flex; justify-content: center; gap: 12px; margin: 18px 0; }
164:.pin-dot {
165-  width: 18px; height: 18px; border-radius: 50%;
166-  background: var(--panel-2); border: 1px solid var(--border);
167-}
168:.pin-dot.filled { background: var(--accent); border-color: var(--accent); }
169:.pin-dot.error { background: var(--bad); border-color: var(--bad); }
170:.keypad {
171-  display: grid;
172-  grid-template-columns: repeat(3, 1fr);
173-  gap: 10px;
174-  max-width: 320px;
175-  margin: 0 auto;
--
177:.keypad button {
178-  height: 64px;
179-  font-size: 24px;
180-  border-radius: 12px;
181-  border: 1px solid var(--border);
182-  background: var(--panel-2);
--
186:.keypad button:active { transform: scale(0.96); background: #2a3854; }
187:.keypad button.key-fn { font-size: 15px; color: var(--muted); }
188:.pin-feedback { min-height: 24px; margin-top: 14px; font-size: 15px; }
189:.pin-feedback.ok { color: var(--ok); }
190:.pin-feedback.err { color: var(--bad); }
191-
192:.kiosk-summary {
193-  margin-top: 18px;
194-  display: flex;
195-  gap: 10px;
196-  flex-wrap: wrap;
197-}
198:.summary-chip {
199-  border: 1px solid var(--border);
200-  border-radius: 999px;
201-  padding: 8px 14px;
202-  font-size: 14px;
203-  background: var(--panel);
--
205:.summary-chip b { font-variant-numeric: tabular-nums; }
206-
207-/* ---------- teacher ---------- */
208-.teacher-controls { display: flex; align-items: flex-end; gap: 10px; }
209-.teacher-tabs { display: flex; gap: 6px; margin-bottom: 14px; border-bottom: 1px solid var(--border); }
210-.tab-btn {
--
217:.toolbar { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; margin-bottom: 12px; }
218:.toolbar .spacer { flex: 1; }
219:.kbd-hint { color: var(--muted); font-size: 12px; }
220-kbd {
221-  background: var(--panel-2); border: 1px solid var(--border);
222-  border-radius: 4px; padding: 1px 6px; font-size: 11px; font-family: inherit;
223-}
224-
--
268:.history-table { width: 100%; border-collapse: collapse; margin-top: 14px; }
269:.history-table th, .history-table td { text-align: left; padding: 8px 10px; border-bottom: 1px solid var(--border); font-size: 13px; }
270:.history-table th { color: var(--muted); font-size: 11px; text-transform: uppercase; }
271-
272-.empty-state {
273-  text-align: center; color: var(--muted); padding: 60px 20px;
274-  border: 2px dashed var(--border); border-radius: var(--radius);
275-}


=== STDERR ===
