Command: export PATH="/workspace/repo/.helix-venv/bin:/workspace/repo/venv/bin:/workspace/repo/.venv/bin:/workspace/.helix-venv/bin:$PATH" && cd /workspace/repo && sed -n '1,120p' styles.css
Cwd: /workspace/repo

=== STDOUT ===
:root {
  --bg: #0f1420;
  --panel: #1a2233;
  --panel-2: #212c42;
  --border: #2e3b57;
  --text: #e8eefb;
  --muted: #93a1bd;
  --accent: #4f8cff;
  --accent-2: #3b6fd4;
  --ok: #2ecc8f;
  --warn: #f5b83d;
  --bad: #ff6b6b;
  --late: #f5b83d;
  --radius: 12px;
  --shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
}

* { box-sizing: border-box; }

html, body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--text);
  font-family: "Segoe UI", system-ui, -apple-system, Roboto, Helvetica, Arial, sans-serif;
  height: 100%;
}

.hidden { display: none !important; }

.topbar {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 10px 18px;
  background: var(--panel);
  border-bottom: 1px solid var(--border);
  position: sticky;
  top: 0;
  z-index: 20;
}

.brand { display: flex; align-items: center; gap: 10px; }
.brand-mark {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 30px;
  height: 30px;
  border-radius: 8px;
  background: var(--accent);
  color: #fff;
  font-weight: 800;
}
.brand-name { font-weight: 700; letter-spacing: 0.3px; }

.mode-switch { display: flex; gap: 8px; margin-left: 12px; }
.mode-btn {
  padding: 8px 16px;
  border-radius: 999px;
  border: 1px solid var(--border);
  background: transparent;
  color: var(--muted);
  font-size: 14px;
  cursor: pointer;
}
.mode-btn.active {
  background: var(--accent);
  border-color: var(--accent);
  color: #fff;
  font-weight: 600;
}

.topbar-right { margin-left: auto; }
.clock { font-variant-numeric: tabular-nums; color: var(--muted); font-size: 14px; }

.app { padding: 18px; max-width: 1280px; margin: 0 auto; }

.view-head {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 14px;
}
.view-head h1 { margin: 0; font-size: 22px; }
.view-sub { margin: 4px 0 0; color: var(--muted); font-size: 14px; }

/* ---------- buttons ---------- */
.btn {
  border: 1px solid var(--border);
  background: var(--panel-2);
  color: var(--text);
  border-radius: 10px;
  padding: 10px 16px;
  font-size: 15px;
  cursor: pointer;
  transition: transform 0.06s ease, background 0.15s ease;
}
.btn:hover { background: #2a3854; }
.btn:active { transform: scale(0.98); }
.btn-primary { background: var(--accent); border-color: var(--accent); color: #fff; font-weight: 600; }
.btn-primary:hover { background: var(--accent-2); }
.btn-danger { background: transparent; border-color: var(--bad); color: var(--bad); }
.btn-danger:hover { background: rgba(255, 107, 107, 0.12); }
.btn-ghost { background: transparent; }
.btn-sm { padding: 6px 10px; font-size: 13px; }
.btn[disabled] { opacity: 0.5; cursor: not-allowed; }

/* ---------- fields ---------- */
.field { display: inline-flex; flex-direction: column; gap: 4px; }
.field-label { font-size: 12px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; }
input[type="text"], input[type="number"], input[type="date"], select {
  background: var(--panel-2);
  border: 1px solid var(--border);
  color: var(--text);
  border-radius: 8px;
  padding: 9px 12px;
  font-size: 15px;


=== STDERR ===
